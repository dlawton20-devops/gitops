defaultNamespace: monitoring
targetNamespace: monitoring

# CRDs should be deployed first
helm:
  chart: prometheus-operator-crds
  repo: https://prometheus-community.github.io/helm-charts
  version: 0.0.1

targets:
- clusterSelector:
    matchLabels:
      env: dev 