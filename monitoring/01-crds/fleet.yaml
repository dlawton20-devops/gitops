defaultNamespace: monitoring
targetNamespace: monitoring

# 01 - CRDs first
# Chart.yaml now references Harbor repository
helm:
  chart: prometheus-operator-crds
  version: 0.0.1
  helmSecretName: harbor-secret

targets:
- clusterName: local  # Match by exact cluster name
# Alternative: clusterSelector with env=dev label
# - clusterSelector:
#     matchLabels:
#       env: dev 